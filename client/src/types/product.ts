export type Product = {
  id: number;
  name: string;
  price: number;
  category: string;
  description: string;
  photoUrl: string;
};
export type ProductCardProps = {
  name: string;
  price: number;
  category: string;
  description: string;
  photoUrl: string;
};
